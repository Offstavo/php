<?php
    // Constants are similar to the variable except once they defined, they can never be undefined or changed.
    // Constanst using define() 1
    // define(name, value, case-insensitive)  
    // Constants dont follow variable scoping rules
    define("MESSAGE","Hello Java.T.Point PHP");
    echo MESSAGE;
?>